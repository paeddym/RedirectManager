const express = require("express");
const app = express();
const fs = require("fs");
const port = 8080;
const filename = __dirname + "/redirects.json";

app.get();

app.get();

app.delete();

app.post();